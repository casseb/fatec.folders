#include <stdio.h>
#include <stdlib.h>
#include "bycasseb.h"

void main()
{
    char teste = 'c';
    int nTeste = teste;
    printf("%i",nTeste);

    /*int quantNumeros,i,j,aux;
    quantNumeros = capturaInt("Digite quantos numeros deseja ordenar: ");
    int numeros[quantNumeros];

    for(i=0;i<quantNumeros;i++){
        numeros[i] = capturaInt("Digite o numero: ");
    }

    for(i=0;i<quantNumeros;i++){
        for(j=i+1;j<quantNumeros;j++){
            if(numeros[j]<numeros[i]){
                aux = numeros[i];
                numeros[i] = numeros[j];
                numeros[j] = aux;
            }
        }
    }

    for(i=0;i<quantNumeros;i++){
        printf(" %i",numeros[i]);
    }*/

}
